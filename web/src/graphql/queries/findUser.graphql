query FindUser($targetUsername: String!) {
  findUser(targetUsername: $targetUsername) {
    errors {
      field
      message
    }
    user {
      _id
      username
      image
      createdAt
      badge
      dayStreak
      cardStudied
      followers {
        _id
        username
      }
    }
  }
}
