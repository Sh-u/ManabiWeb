query FindDeck($_id: Int!) {
  findDeck(_id: $_id) {
    decks {
      _id,
      user {
        _id,
        username
      },
      posts {
        _id,
          
      },
      title,
      createdAt,
      updatedAt,
      subscribers {
        _id
      }
    }
  }
}
