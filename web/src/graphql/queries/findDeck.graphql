query FindDeck($_id: Int!) {
  findDeck(_id: $_id) {
    decks {
      _id,
      author {
        _id,
        username
      },
      posts {
        _id,
          
      },
      title,
      createdAt,
      updatedAt
    }
  }
}
