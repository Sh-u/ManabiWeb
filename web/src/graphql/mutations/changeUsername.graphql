mutation ChangeUsername($newUsername: String!){
  changeUsername(newUsername: $newUsername) {
    user {
      _id,
      username,
      email
    }
    errors {
      field,
      message,
    }
  }
}